import { ConfirmWindowDirective } from './confirm-window.directive';

describe('ConfirmWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmWindowDirective();
    expect(directive).toBeTruthy();
  });
});
